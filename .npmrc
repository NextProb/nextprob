ignore-scripts=true
# After npm ci, run:
#   npm rebuild electron --ignore-scripts=false       # downloads Electron binary
#   npx electron-rebuild -o better-sqlite3,node-pty   # builds native deps against Electron's Node ABI
